#include "main.h"

/**
 * print_alphabet - the function that prints the alphabets in lowercase
 * return: 0
 */

void print_alphabet(void);
{
	char p;

	for (p = 'a'; p <= 'z'; p++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
